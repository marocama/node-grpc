# node-grpc

Exemplo de uso de comunicação gRPC manipulando uma lista de usuários estáticos.

## Servidor

`node server` 

## Cliente

`node client`

### Services

`Insert(User)`
`Find(User)`
`FindAll(Empty)`

#### Incluir

`Update(User)`
`Delete(User)`